# Knitr and RStudio cache artifacts
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# Quarto's cache directory (stores pre-rendered results for speed)
.quarto/
# R Markdown cache folders
*_cache/
# The rendered site output (HTML files, assets, etc.)
_site/
# Active files for Readme preview for editing
README_files/
# Mac OS directory thing
.DS_Store

# GitHub Pages deployment branch (if checked out locally)
gh-pages/

# Any rendered products - not needed when using GitHub Pages deployment
*.html

# Logs and temporary files created during rendering
*.log
*.out
*.toc
*.aux
*.nav
*.snm
*.vrb
*.blg
*.bbl

